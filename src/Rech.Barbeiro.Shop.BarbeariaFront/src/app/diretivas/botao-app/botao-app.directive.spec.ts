import { BotaoAppDirective } from './botao-app.directive';

describe('BotaoAppDirective', () => {
  it('should create an instance', () => {
    const directive = new BotaoAppDirective();
    expect(directive).toBeTruthy();
  });
});
